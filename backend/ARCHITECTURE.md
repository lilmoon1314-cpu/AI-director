# Retired compatibility route

See root `ARCHITECTURE.md` for ownership and `docs/design-docs/` for durable backend reasoning. Current
routes and schemas are owned by FastAPI/OpenAPI, ORM models, and migrations.
