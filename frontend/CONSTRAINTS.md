# Retired compatibility route

Observable UI behavior is owned by `docs/product-specs/`; durable implementation boundaries are owned
by `docs/design-docs/`; executable constraints are owned by frontend code, tests, lint, and type config.
