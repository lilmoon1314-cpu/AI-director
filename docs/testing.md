# Retired compatibility route

Tests and their configuration own executable test behavior. Use the risk-based rules in `AGENTS.md`;
the ExecPlan protocol in `docs/PLANS.md` owns complex-task validation and progress.
