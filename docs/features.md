# 功能清单（第 1 批 MVP）

> 状态机：`not_started` → `active` → `passing` / `blocked`。
> 规则：同时只激活一个功能项；状态由验证脚本自动更新，禁止手工修改（AGENTS.md 功能清单规则）。
> 验证层级（docs/testing.md）：L1 单元 + L2 集成每功能必须；L3 E2E 仅"跨组件"功能必须。跳过任何必须层级 = 未完成。
> 每个功能实现时创建 `docs/tests/FXX_<name>.md` 测试文档，DoD 见 docs/testing.md §2。

| ID  | 功能            | 行为描述                                                                                                                            | 跨组件                                      | 验证命令（必须层级）                                                                                                   | 状态           |
| --- | ------------- | ------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------ |
| F01 | 项目初始化         | make setup 从零安装依赖/生成 .env/完成迁移；make dev 启动前后端；make test 示例测试通过                                                                  | 否                                        | `make setup && make test && make check`（L1 + 架构基线）                                                           | passing      |
| F02 | 实体 CRUD API   | POST/GET/PATCH/DELETE /api/entities：properties 按类型校验、id 系统生成、删除校验关系引用（应用层 ReferentialError + DB 层外键 ON DELETE RESTRICT 兜底）、@ 检索；首个迁移建齐 entities+relationships 两表并声明外键                                                      | 否                                        | L1: `pytest tests/unit/test_entities_service.py`；L2: `pytest tests/integration/test_entities_api.py`         | passing |
| F03 | 关系 CRUD API   | POST/GET/PATCH/DELETE /api/relations：端点存在性校验、自环/重复关系拒绝、known\_by 成员校验                                                           | 是（entities+relations）                    | L1: `pytest tests/unit/test_relations_service.py`；L2: `pytest tests/integration/test_relations_api.py`；L3: `pytest tests/e2e/test_relations_flow.py`                                                   | passing |
| F04 | 三视角过滤图查询      | GET /api/graph?perspective=author/character/audience：author 全量；character 按 known\_by 过滤且不泄露被过滤实体名；audience 按 audience\_known 过滤 | 是（entities+relations+perspectives）       | L1: `pytest tests/unit/test_perspectives_service.py`；L2/L3: `pytest tests/integration tests/e2e -k graph`    | passing |
| F05 | 前端图谱工作台       | G6 力导向图渲染实体/关系（缩放/拖拽/点击），节点详情面板（毛玻璃样式），CRUD 表单                                                                                  | 是（前后端联调）                                 | L1/L2: `pnpm test:unit && pnpm test:integration`；L3: Playwright `pnpm test:e2e`                              | passing |
| F06 | 视角切换 UI       | 一键切换三视角；character 视角选择角色（作者、角色、观众）；切换后图数据按视角刷新                                                                                  | 是（前后端联调）                                 | L1/L2: `pnpm test:unit && pnpm test:integration`；L3: Playwright `pnpm test:e2e`（场景含三视角切换 perspective.spec）                                                                                       | passing |
| F07 | @ 实体选择器       | 输入 @ 触发防抖检索，选择插入引用，提示该实体对当前视角是否可见                                                                                               | 是（前后端联调）                                 | L1: `pytest tests/unit/test_entities_service.py`；L1/L2 前端: `pnpm test:unit && pnpm test:integration`；L3: Playwright `pnpm test:e2e`（场景含 @ 检索 entity-picker.spec）                                                                                       | passing |
| F08 | 资产管理       | 独立资产库（data/assets.db，HTML 形态存储）；图片上传（白名单/上限/uuid 重命名/流式写盘）；通用资产 CRUD（分类/标题/描述/自由属性/多图）；项目资产=实体按类型分组卡片（缩略图/名称/概述），实体 HTML 资产页惰性生成、过期再生、删除联动清扫；工作台「图谱｜资产管理」双页 + 内嵌 HTML 查看器                                                      | 是（assets+entities+前后端联调）                       | L1: `pytest tests/unit/test_assets_service.py`；L2/L3: `pytest tests/integration tests/e2e -k asset`；前端: `pnpm test:unit && pnpm test:integration` + Playwright `pnpm test:e2e`         | passing |
| F10 | Agent 对话与确认写入 | POST /api/agent/chat SSE 流式回复（上下文经视角过滤）；propose 生成结构化草案；confirm 确认后落库；AgentHome 主页 + AgentDock 侧边栏（DESIGN.md §5.4/§5.5，执行顺序挪至 F11/F12 之后，编号不变）                                                           | 是（agent+entities+relations+perspectives） | L1: `pytest tests/unit/test_agent_service.py`；L2/L3: `pytest tests/integration tests/e2e -k agent`（LLM mock） | passing |
| F11 | 多项目底座 | projects 表 + Alembic 迁移（entities/relationships 增 project_id + 默认项目打包存量数据）；项目 CRUD API（计数器维护/改名/级联删除+资产清扫兜底）；既有端点增 project 维度过滤（查询参数渐进迁移，DESIGN.md §8）；前端 react-router 路由化 + 项目首屏 + 顶栏切换器 + store 重置矩阵（DESIGN.md §4/§5.1/§7） | 是（projects+entities+relations+perspectives+assets+前后端联调） | L1: `pytest tests/unit/test_projects_service.py`；L2: `pytest tests/integration/test_projects_api.py`；L3: `pytest tests/e2e -k project`；前端: `pnpm test:unit && pnpm test:integration && pnpm test:e2e` | passing |
| F12 | 工作台导航与资产页重构 | 资产页两级钻取（7 类型库墙→类型卡片墙→实体 HTML）+ 分区独立搜索 + 空状态引导 + 通用资产表单 modal 化 + 卡片规范统一与 testid 迁移（DESIGN.md §5.3/§9/§10） | 是（前后端联调） | 前端: `pnpm test:unit && pnpm test:integration && pnpm test:e2e`；后端回归: `pytest tests/integration -k asset` | passing |
| F13 | Agent 对话体验升级 | 真流式 SSE（stream=True 逐 chunk，废除伪分块）+ 思考过程可视化（reasoning_content 灰字折叠卡，DESIGN §13.1）+ usage/上下文容量窗口（usage 事件 + UsageBar）+ 会话/记忆文档删除端点与 UI + 记忆文档编辑大弹窗（max-w-5xl 三栏）+ 指导类文档项目内唯一（409）+ Dock 会话下拉/＋新会话/Esc（F10 偏离清单兑现） | 是（agent+前后端联调） | L1: `pytest tests/unit/test_agent_service.py tests/unit/test_agent_llm.py tests/unit/test_agent_memory_docs.py`；L2: `pytest tests/integration -k agent`；L3: `pytest tests/e2e -k agent`（LLM mock 流式桩）；前端: `pnpm test:unit && pnpm test:integration` | active |
| F14 | Agent 写入工具链 | 写入类工具（create_entity/update_entity/create_relation/create_memory_doc/write_doc_section，执行仅登记 pending）+ 轮末统一确认（done 携带待写入清单→前端确认卡→approve 服务端二次校验落库→图谱/文档失效刷新，DECISIONS 2026-09-08 OQ-8）+ agent_pending_writes 表 + pending 上限 config + propose 标注 legacy | 是（agent+entities+relations+前后端联调） | L1: `pytest tests/unit/test_agent_service.py tests/unit/test_agent_tools.py`；L2: `pytest tests/integration -k agent`；L3: `pytest tests/e2e -k agent`；前端: `pnpm test:unit && pnpm test:integration` | not_started |
| F15 | 长期记忆体系 | memory_docs 类型学（guide 指导类项目唯一 + work 作品类多实例：outline/screenplay/episode_script/storyboard 模板注册表，DESIGN §13.3）+ 存量项目惰性补建指导文档 + global_memory_entries 表与全局画像自动沉淀（LLM_MODEL_LIGHT 提炼，config 开关）+ /global-memory 透明可编辑页 + search_session_summaries 跨会话检索工具 + 画像摘要 system 注入 | 是（agent+projects+前后端联调） | L1: `pytest tests/unit/test_agent_service.py tests/unit/test_agent_memory_docs.py`；L2: `pytest tests/integration -k agent`；L3: `pytest tests/e2e -k agent`；前端: `pnpm test:unit && pnpm test:integration` | not_started |
| F16 | 多系列剧情线 | series 表 + entity_series 多对多（无关联=共享底座）+ memory_docs.series_id 挂载剧情文档 + 图查询/资产页/agent 上下文按系列过滤（series 视图=共享∪该系列，DECISIONS 2026-09-08 OQ-9）+ 系列管理 UI（新建/切换/徽标）+ agent 对话 series 参数 | 是（agent+entities+perspectives+projects+前后端联调） | L1: `pytest tests/unit/test_projects_service.py tests/unit/test_perspectives_service.py`；L2: `pytest tests/integration -k "series or graph"`；L3: `pytest tests/e2e -k series`；前端: `pnpm test:unit && pnpm test:integration` | not_started |

注：pytest 命令均在 backend/ 目录下执行（make test-\* 已封装）。

## 初始化验收清单（INIT.md，F01 完成条件）

- [x] `make setup` 从零开始能成功（Windows 经 `python scripts/task.py setup` 等价执行）
- [x] `make test` 至少有一个测试通过（后端 5 + 前端 2，含架构检查基线）
- [x] 新的 agent 会话能只看仓库回答"怎么跑"和"怎么测"（AGENTS.md 首次运行命令）
- [x] 任务分解文件存在且有至少 3 个任务（本清单，10 项）
- [x] 所有内容已提交到 git（F01 checkpoint）

