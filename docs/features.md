# Retired compatibility route

Machine-readable feature acceptance state is owned by `feature_list.json`. Query it with
`python scripts/task.py verify --list` and optional `--status` or `--category` filters.
