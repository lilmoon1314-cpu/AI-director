# V4.1 R2 acceptance — 2026-09-19

R2 is complete at the backend/API boundary. Baseline R0/R1 work was preserved and pushed as c87c5e5
before R2 began. No R3, media provider, or Agent G/H implementation is included.

## Delivered behavior

- Independent approval lifecycle and VALID/STALE/BLOCKED freshness.
- Sole Lineage dependency writes with legacy API compatibility and retained historical tables.
- Validated block/revision references, project isolation, cycle prevention, policy impact preview,
  precise baseline invalidation, and derived transitive blockers.
- Explicit reverse proposals, idempotent decisions, production-owner semantic checks, compatibility
  validation, content-identical rebase revisions, immutable revert, and append-only review audit.
- Revert preserves current incoming baselines pending review, avoiding false validity of old content.
- Generated OpenAPI types and machine-enforced domain boundaries.

## Evidence

| Check | Result |
|---|---|
| New Lineage API + inhabited migration tests | 18 passed |
| Existing Artifact API/E2E, Production, Skills, workspace navigation, architecture | 27 passed |
| Import contracts | 18 kept, 0 broken |
| Targeted mypy / Ruff lint / formatting / Git whitespace | Passed |
| OpenAPI generation / frontend TypeScript | Passed with installed Node CLIs |
| Actual database upgrade / foreign keys / Alembic metadata | Passed |

New acceptance tests exercise every invalidation policy, a dependency baseline separated by an
unrelated revision, approval retained when stale, transitive blocking/unblocking, cross-project and
forged references, cycles, historical baseline creation, legacy write cutover, proposal rejection,
compatibility success/failure, stale-base conflicts, concurrent duplicate acceptance, immutable revert,
and rollback injected after both target write and rebase.

Test paths: `backend/tests/integration/test_lineage_api.py` and
`backend/tests/integration/test_lineage_migration_upgrade.py`. No historical full suite/mutation run.
Existing suites emitted the existing Starlette/httpx deprecation warning. The local pnpm wrapper
attempted an unnecessary dependency reinstall and stopped on its non-interactive guard; using the
already installed OpenAPI/TypeScript Node CLIs completed validation without changing dependencies.

## Data and screenshot preference

Actual app.db was upgraded from f4b7c8d9e0a1 to a5557d421f40 after SQLite backup to
`backend/data/backups/pre-v4-r2-20260919-173152.db`. All original columns/rows across 35 existing tables
matched the backup after upgrade. The migration is additive; old dependency tables and revisions
remain. Database, backup, .env, and assets are excluded from the commit.

The default screenshot rule is saved in the user's global `C:/Users/18539/.codex/AGENTS.md` and the
repository rule. `test/pic_test/` is available for future frontend layout/visual acceptance evidence.
This R2 task did not change frontend layout and therefore did not produce layout screenshots.

The completed execution plan is `docs/exec-plans/completed/v4-r2-lineage.md`. Stop at the R3 boundary.
