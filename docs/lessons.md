# Retired compatibility route

Machine checks in `backend/tests/architecture/` own automated regressions, bounded files in
`docs/design-docs/` own durable reasoning, and Git owns ordinary failure history.
