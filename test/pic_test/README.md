# Frontend acceptance screenshots

Save final layout/visual acceptance screenshots here. Use descriptive names including the task,
page or state, and viewport (for example `v4-r2-review-desktop-1440x900.png`). Link evidence in the
task's final response. This directory is retained even when a task has no frontend layout changes.
