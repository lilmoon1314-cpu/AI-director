# Retired compatibility route

This former constraint registry is no longer authoritative. Agent-wide rules live in `AGENTS.md`,
durable boundaries in `docs/design-docs/`, and enforceable rules in code, tests, and tool config.
